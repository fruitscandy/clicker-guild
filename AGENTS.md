# Clicker Guild 병렬 작업 규칙

## 현재 목표

- 제한된 시간 안에 실행 가능한 클릭커 RPG MVP를 완성한다.
- 다른 PC가 담당하는 전투 화면의 그래픽과 스킬 작업을 보호한다.
- 각 작업은 독립 브랜치 또는 worktree에서 수행하고, 통합 담당자가 검수 후 병합한다.

## 새 세션 시작 프로토콜

사용자가 현재 작업을 다시 설명하지 않아도 모든 새 에이전트 세션은 파일을 수정하기 전에 다음 순서로 상태를 파악한다.

1. 이 `AGENTS.md`를 끝까지 읽는다.
2. `docs/coordination/README.md`와 `docs/coordination/claims/*.md`를 읽는다.
3. `git status --short --branch`, 최근 커밋, 현재 브랜치와 사용 가능한 원격 브랜치를 확인한다.
4. GitHub 연결을 사용할 수 있으면 열린 이슈·PR에서 `in-progress`, 담당 영역, 소유 경로를 확인한다.
5. 오래 실행 중인 작업이면 GitHub Discussions의 Agent Coordination 글에서 새 질문, 결정, `@mention`을 확인한다.
6. 활성 claim과 파일 소유 범위가 겹치지 않는 `ready` 작업 중 우선순위가 가장 높은 하나를 선택한다.
7. 수정 전에 자기 작업의 claim 파일을 새로 만들고 작업 ID, 브랜치, 소유 경로, 읽기 전용 경로, 완료 조건을 기록한다.
8. 안전하게 선점할 작업이 없으면 임의 구현을 시작하지 말고 읽기 전용 조사·검증을 수행해 차단 요소를 보고한다.

새 세션은 다른 세션의 대화 기억을 전제로 하지 않는다. 저장소 claim과 원격 이슈/PR 상태가 세션 간 공용 기억이다.

GitHub가 연결된 세션에서는 GitHub 이슈와 PR이 최우선 공용 기억이다. 로컬 claim은 원격 확인이 불가능할 때 충돌을 줄이는 보조 캐시다.

## GPT Work 요청 처리 규칙

두 개발자는 주로 각자의 GPT Work에 자연어로 요청한다. 에이전트는 다음처럼 GitHub 작업으로 변환한다.

- “테스트하다 버그를 찾았어” → 피드백 이슈에 댓글을 추가하거나 재현 가능한 별도 버그 이슈 생성
- “이 기능도 있었으면 좋겠어” → 기존 중복을 검색한 뒤 P1/P2 개선 이슈 생성
- “다음 작업 진행해줘” → 열린 이슈·PR·CI를 조회하고 선행 조건을 만족한 `ready` 이슈를 선점
- “현재 상황 알려줘” → 활성 이슈, 열린 PR, 차단 작업, 최근 CI를 요약
- “이 작업 검토해줘” → 해당 PR의 diff, 테스트, 리뷰 댓글과 미해결 사항 확인

구현 세션은 이슈 없이 임의 작업을 시작하지 않는 것을 원칙으로 한다. 작은 긴급 수정도 먼저 이슈 또는 PR 설명에 목적과 소유 경로를 기록한다.

GitHub 이슈 쓰기가 `403 Resource not accessible by integration` 등으로 실패하면 작업 선점이 완료되지 않은 것이다. 다른 세션과 충돌할 수 있는 구현을 시작하지 말고, 연결의 Issues 쓰기 권한 재승인이 필요하다고 사용자에게 알린다.

## 세션 간 의사소통

- 같은 메인 세션 아래의 서브에이전트는 메시지로 직접 조율할 수 있다.
- 서로 독립된 Codex 작업이나 다른 PC는 직접 메시지 버스를 공유한다고 가정하지 않는다.
- 독립 작업 간 상태 공유는 `docs/coordination/claims/`와 GitHub 이슈·PR을 사용한다.
- PC가 다르면 claim 변경을 작은 커밋으로 push해야 다른 PC가 볼 수 있다.
- 긴 진행 설명은 이슈/PR에 남기고, claim 파일에는 현재 상태와 파일 소유권만 간결하게 유지한다.
- 중앙 검토 세션은 열린 이슈·PR·CI와 피드백 수집 이슈를 주기적으로 확인해 중복 병합, 우선순위 조정, 차단 해제, `ready` 작업 분리를 수행한다.

## 에이전트 간 장기 작업 토론

- 두 사용자 계정 사이의 설계 토론과 장기 질문은 GitHub Discussions의 `Agent Coordination` 글을 사용한다.
- 작업 상태와 책임은 Issue, 코드별 의견은 PR 리뷰, 여러 이슈에 걸친 판단은 Discussion에 남긴다.
- 글이나 댓글에는 관련 이슈, 브랜치, 기준 커밋, 소유 경로, 질문, 선택지, 추천안, 응답 필요 시점을 적는다.
- 다른 개발자의 GitHub 계정을 `@mention`해 알림 대상이 명확하도록 한다.
- 답변을 기다리는 동안 해당 이슈를 Blocked로 표시하고, 같은 파일을 건드리지 않는 다른 Ready 작업을 진행한다.
- 에이전트는 긴 작업의 주요 경계(시작, 설계 변경, 차단, 테스트 완료, 인계)에서 Discussion과 Issue를 다시 확인한다. 초 단위 반복 조회는 하지 않는다.
- 결정이 내려지면 Discussion의 결론을 관련 Issue와 PR에도 복사해 후속 에이전트가 토론 전체를 읽지 않아도 되게 한다.

## 작업 레인과 소유 범위

### 전투 그래픽·스킬 레인 — 다른 PC 소유

- 전투 화면 그래픽, 전투용 캐릭터·몬스터 에셋, 스킬 연출과 관련된 진행 중 파일은 명시적인 인계 전까지 수정하지 않는다.
- `docs/experimental-asset/`의 기존 이미지와 애니메이션은 읽기 전용 자료로 취급한다.
- 전달받은 결과는 직접 재작성하지 말고 통합 브랜치에서 계약과 규격을 먼저 확인한다.

### 비전투·기반 레인 — 이 PC 소유

- MVP 범위와 누락 항목 조사
- 성장, 경제, 영입, 스테이지 진행, 저장 데이터 설계
- 프로젝트 구조, 에셋 로더 계약, 테스트·빌드·배포 기준
- 작업 보드와 통합 체크리스트

## 동시 작업 원칙

1. 하나의 작업은 하나의 브랜치 또는 worktree를 사용한다.
2. 두 작업이 같은 파일을 동시에 수정하지 않는다.
3. 공용 설정 파일과 의존성 파일은 통합 담당자만 변경한다.
4. 작업 시작 전에 수정할 경로를 선언하고, 범위를 벗어나면 먼저 인계한다.
5. 큰 이미지와 생성 원본은 교체하지 말고 새 파일로 제출한 뒤 검수한다.
6. 커밋은 하나의 목적만 담고, 산출물·검증 결과·남은 위험을 함께 보고한다.
7. 활성 claim에 적힌 소유 경로는 해당 작업의 명시적 인계 전까지 수정하지 않는다.
8. 공용 파일을 변경해야 하면 직접 수정하기 전에 claim에 요청을 남기고 통합 담당자가 반영한다.

## 작업 종료 프로토콜

1. 완료 조건에 맞춰 테스트·빌드·수동 검증을 수행한다.
2. claim의 상태를 `completed`, `blocked`, `handed-off` 중 하나로 변경한다.
3. 변경 파일, 검증 결과, 기준 커밋, 알려진 문제와 다음 작업을 claim에 기록한다.
4. 다른 PC가 결과를 사용해야 하면 브랜치를 push하고 GitHub 이슈/PR에 인계 내용을 남긴다.
5. 후속 세션이 바로 시작할 수 있도록 새 `ready` 작업 또는 남은 차단 요소를 명시한다.

## 완료 보고 형식

- 담당 작업과 목표
- 변경한 파일 목록
- 실행하거나 확인한 검증
- 통합 시 필요한 조치
- 알려진 문제와 후속 작업

## 통합 게이트

- 다른 작업의 소유 파일을 침범하지 않았는가
- 문서 또는 manifest에 적힌 파일 경로가 실제 파일과 일치하는가
- 게임 실행과 저장 데이터 호환성을 깨뜨리지 않는가
- 임시 에셋과 확정 에셋이 구분되어 있는가
- MVP에 필요하지 않은 기능이 핵심 경로를 지연시키지 않는가
