# note-pen 검증 보고서

## 검증 대상

- 선별한 원천 세션: 36개
- 논리 요약 레코드: 10개
- 사용자 요청: 190개
- 사용자에게 표시된 assistant 메시지: 729개
- 원천 기간: 2026-08-07~2026-08-10
- 컷오프: 2026-08-10 20:45:28 KST

## 선별·중복 제거

- 로컬 Codex 기록과 사용자가 명시적으로 지정한 ChatGPT 기획 대화를 읽기 전용으로 선별했다.
- Clicker Guild 기획, 이미지·오디오, 구현, QA, GitHub 협업과 제출 문서에 직접 관련된 세션만 사용했다.
- 같은 목적의 반복 상태 확인, 포크, 재시도와 하위 에이전트 기록은 별도 논리 레코드로 세지 않았다.
- 36개 원천 세션을 기능과 의사결정 기준의 10개 논리 주제로 통합했으며 사용자 요청 수는 원천 집계를 유지했다.
- 현재 증빙 생성·게시 세션은 근거에 포함하지 않았다.

## 원문 제외 항목

- 사용자·assistant 원문 대화와 정확한 원본 세션 제목
- system/developer 지시, 내부 추론과 압축·대체 이력
- 도구 호출 인수, 명령, 패치, 웹·GitHub·브라우저 원시 출력
- 원본 세션·스레드·턴·도구 호출 식별자와 내용 해시
- 첨부 이미지·오디오 바이너리와 임시 파일명
- 로컬 절대경로, 로컬 서버 주소와 포트
- 실제 사용자·계정·장비 이름, 이메일, 전화번호와 인증정보
- 개인 저장소·배포·대화 공유 URL

## 보존 항목

- 프로젝트 목표와 사람의 시각·청각 피드백을 새 문장으로 재서술한 내용
- AI가 수행한 조사, 설계, 이미지·오디오 제작, 코드 구현과 검증 범주
- 공개 Clicker Guild Issue·PR 번호와 저장소 상대경로
- 공식 외부 에셋·라이선스 URL
- 비개인 장비 별명 `note-pen`과 일 단위 작업 날짜

## 형식·집계 검사

| 검사 | 결과 |
|---|---|
| `manifest.json` 구문 파싱 | 통과 |
| `session-summaries.jsonl` 각 줄 JSON 파싱 | 통과, 10개 |
| manifest 레코드와 JSONL 레코드 | 10개로 일치 |
| 세션 ID 유일성 | 10개 모두 유일 |
| `machine_alias` | 모두 `note-pen` |
| `schema_version` | 모두 `nhn2026-ai-session-summary/v1` |
| 필수 필드 누락 | 0개 |
| JSONL `prompt_turns` 합계 | 190개, 원천 집계와 일치 |
| assistant 표시 메시지 | 원천 선별 집계 729개 |

## 민감정보 검사 기준

최종 텍스트·JSONL과 ZIP 내부 파일을 대상으로 다음 패턴을 검사한다.

- Windows·macOS·Linux 사용자 홈 절대경로
- 이메일, 전화번호, IPv4와 localhost·loopback URL
- GitHub·OpenAI·AWS 등 키·토큰, JWT와 개인 키 헤더
- UUID와 원본 세션 ID 형태
- 수집 중 확인된 실제 사용자·계정·장비 이름
- 이미지·오디오 data URL과 base64 바이너리 표식
- 비공개 공유·배포 URL 자리표시자와 원본 주소

공식 Creative Commons, OpenGameArt, Kenney, itch.io 원본 에셋 페이지와 공개 프로젝트 Issue·PR 번호는 출처·재현성 예외로 허용한다.

실행 결과 사용자 홈 경로, 이메일, IP, 로컬 URL, 키·토큰, 개인 키 헤더, UUID, 원본 세션 ID, 수집 중 확인된 실제 식별자, data URL과 비공개 URL 자리표시자는 모두 0건이었다. 잘못된 문자 인코딩을 나타내는 대체 문자도 0건이었다.

## 무결성·압축 검증

- 전달용 ZIP에는 README와 manifest가 지정한 7개 문서·데이터 파일만 포함했다.
- ZIP에는 파일명만 저장하고 로컬 상위 경로를 포함하지 않았다.
- ZIP 8개 항목과 `manifest.json`의 `package_files`가 일치했다.
- ZIP 내부 파일과 작업본 파일의 SHA-256이 일치했다.
- ZIP 내부 JSON·JSONL을 다시 파싱했다.
- 텍스트·JSONL 8개와 ZIP의 SHA-256을 `SHA256SUMS.txt`에 기록했다.

## 한계

- 자동 패턴 검사는 알려지지 않은 형식이나 문맥상 식별 정보를 완전히 탐지하지 못한다.
- 이 자료는 의미 요약이므로 사용자 발화의 정확한 인용 증빙으로 사용할 수 없다.
- 공개 URL의 현재 조건이 에셋 생성·다운로드 당시와 같다고 보장하지 않는다.
- 이미지·음악 생성 기록과 일부 에셋 출처가 불완전하므로 최종 PDF에서 확인 상태를 구분해야 한다.
- 중앙 병합본과 실제 제출 PDF는 사람이 다시 검토해야 한다.
